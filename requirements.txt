numpy==2.5.2
opencv-python==5.0.0.93
ultralytics==8.4.130

# PyTorch with CUDA 12.6 must come from the PyTorch index, not PyPI
# (PyPI's Windows torch wheels are CPU-only):
#   pip install torch==2.13.0 torchvision --index-url https://download.pytorch.org/whl/cu126
torch==2.13.0
